import type { Metadata } from "next";
import { Saira, Saira_Condensed, Inter } from 'next/font/google';
import "./globals.css";
import "./tailwind.css";
import Footer from "../components/Footer/Footer";
import Navbar from "@/components/Navbar/Navbar";

export const metadata: Metadata = {
  title: "Train Station Fitness",
  description: "Generated by create next app",
};

const saira = Saira({
  subsets: ['latin'],
  display: 'swap',
  weight: ['100', '200', '300', '400', '500', '600', '700', '800', '900'],
  variable: '--font-saira',
});

const sairaCondensed = Saira_Condensed({
  subsets: ['latin'],
  display: 'swap',
  weight: ['100', '200', '300', '400', '500', '600', '700', '800', '900'],
  variable: '--font-saira-condensed',
});

const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
  weight: ['100', '200', '300', '400', '500', '600', '700', '800', '900'],
  variable: '--font-inter',
});

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" data-theme="dark" className="scroll-smooth bg-zinc-900">
      <head>
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      </head>
      <body
        className={`${saira.variable} ${sairaCondensed.variable} ${inter.variable} antialiased min-h-screen bg-black text-base-content`}
      >
        <Navbar />
        {children}
        <Footer />
      </body>
    </html>
  );
}
